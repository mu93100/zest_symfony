{% if field.value %}
    <img src="{{ asset('uploads/medias/' ~ field.value) }}" style="height:2rem; border-radius:0.02rem;">
{% else %}
    <span style="color:#00ff9d;">Aucune image</span>
{% endif %}
    