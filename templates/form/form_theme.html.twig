{% block form_label %}
    {% if label is not same as(false) %}
        {% set labelText = label|default(name)|trans %}
        {% if required %}
            {{ labelText }} *
        {% else %}
            {{ labelText }}
        {% endif %}
    {% endif %}
{% endblock %}
